class LastMessage {
  String displayName;
  String lastMessage;
  DateTime timeStamp;

  LastMessage(
      {required this.displayName,
      required this.lastMessage,
      required this.timeStamp});
}
